<Window x:Class="Vertex.App.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Correo" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="25"/>

        <TextBlock Text="Contraseña" Grid.Row="2" Margin="0,10,0,5"/>
        <PasswordBox Grid.Row="3" PasswordChanged="PasswordBox_PasswordChanged" Height="25"/>

        <Button Content="Login" Grid.Row="4" Margin="0,20,0,0" Height="30" Command="{Binding LoginCommand}"/>
    </Grid>
</Window>
