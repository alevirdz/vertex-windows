<Window x:Class="Vertex.App.Views.PlantillaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:Vertex.App.Converters"
        Title="Vertex"
        Height="700"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F6FA">

    <Window.Resources>
        <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        
        <!-- Estilo para TreeViewItems moderno -->
        <Style TargetType="TreeViewItem">
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal">
                                <ToggleButton x:Name="Expander"
                                              Width="16"
                                              Height="16"
                                              Margin="0,0,4,0"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              Visibility="{TemplateBinding HasItems, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Path x:Name="ExpandPath" Fill="#6366F1" Data="M 0 0 L 8 8 L 0 16 Z" RenderTransformOrigin="0.5,0.5">
                                        <Path.RenderTransform>
                                            <RotateTransform Angle="0"/>
                                        </Path.RenderTransform>
                                    </Path>
                                </ToggleButton>
                                <ContentPresenter x:Name="PART_Header" ContentSource="Header"/>
                            </StackPanel>
                            <ItemsPresenter x:Name="ItemsHost"
                                            Grid.Column="1"
                                            Margin="12,0,0,0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandPath" Property="RenderTransform">
                                    <Setter.Value>
                                        <RotateTransform Angle="90"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= SIDEBAR ================= -->
        <Border Grid.Column="0"
                Background="White"
                BorderBrush="#E5E7EB"
                BorderThickness="0,0,1,0"
                Height="684"
                VerticalAlignment="Top">

            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Logo -->
                <TextBlock Text="VERTEX"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="#6366F1"
                           Margin="0,10"/>

                <!-- Usuario -->
                <StackPanel Grid.Row="1" Margin="0,30,0,20">
                    <TextBlock Text="{Binding User.Name}"
                               FontWeight="SemiBold"
                               FontSize="14"/>
                    <TextBlock Text="{Binding User.Email}"
                               Foreground="Gray"
                               FontSize="11"/>
                </StackPanel>

                <!-- Menú -->
                <TreeView Grid.Row="2"
          ItemsSource="{Binding MenuItems}"
          SelectedItemChanged="TreeView_SelectedItemChanged"
          BorderThickness="0"
          Background="Transparent">
                    <TreeView.Resources>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="Foreground" Value="#333"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Padding" Value="4,2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TreeViewItem">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Orientation="Horizontal">
                                                <ToggleButton x:Name="Expander"
                                              Width="16"
                                              Height="16"
                                              Margin="0,0,4,0"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                              Visibility="{TemplateBinding HasItems, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <Path x:Name="ExpandPath"
                                          Fill="#6366F1"
                                          Data="M 0 0 L 8 8 L 0 16 Z"
                                          RenderTransformOrigin="0.5,0.5">
                                                        <Path.RenderTransform>
                                                            <RotateTransform Angle="0"/>
                                                        </Path.RenderTransform>
                                                    </Path>
                                                </ToggleButton>

                                                <ContentPresenter x:Name="PART_Header" ContentSource="Header"/>
                                            </StackPanel>

                                            <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Margin="12,0,0,0"/>
                                        </Grid>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsExpanded" Value="True">
                                                <Setter TargetName="ExpandPath" Property="RenderTransform">
                                                    <Setter.Value>
                                                        <RotateTransform Angle="90"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TreeView.Resources>

                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal" Margin="4,2">
                                <!-- Ícono -->
                                <TextBlock Text="{Binding Icon}" FontSize="14" Margin="0,0,8,0"/>
                                <!-- Label -->
                                <TextBlock Text="{Binding Label}" FontSize="13" FontWeight="SemiBold"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>



                <!-- Footer -->
                <StackPanel Grid.Row="3"
                            HorizontalAlignment="Center"
                            Margin="0,10"
                            IsEnabled="{Binding IsLoggingOut, Converter={StaticResource InverseBooleanConverter}}">
                    <TextBlock Text="Vertex © 2026"
                               FontSize="11"
                               Foreground="Gray"
                               HorizontalAlignment="Center"/>

                    <!-- Botón Cerrar sesión -->
                    <Button Content="Cerrar sesión"
                            Command="{Binding LogoutCommand}"
                            IsEnabled="{Binding IsLoggingOut, Converter={StaticResource InverseBooleanConverter}}"
                            Margin="0,10,0,0"
                            Padding="6,4"
                            Width="120"
                            Background="#2a3236"
                            Foreground="White"
                            FontWeight="SemiBold"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Center">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#2a3236"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#A0A0A0"/>
                                        <Setter Property="Foreground" Value="#D0D0D0"/>
                                        <Setter Property="Cursor" Value="Arrow"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

            </Grid>
        </Border>

        <!-- ================= CONTENIDO PRINCIPAL ================= -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER SUPERIOR -->
            <Border Background="White"
                    BorderBrush="#E5E7EB"
                    BorderThickness="0,0,0,1">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                   

                    <!-- Avatar -->
                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                Margin="10,0,0,0">
                        <Grid Width="35" Height="35" Margin="0,0,10,0">
                            <Ellipse Fill="#6366F1"/>
                            <TextBlock Text="{Binding UserInitials}"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock Text="{Binding User.Name}"
                                   VerticalAlignment="Center"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- AREA DINAMICA -->
            <Border Grid.Row="1" Padding="20" Background="Transparent">
                <ContentControl x:Name="ContentArea"
                                Content="{Binding CurrentView}" />
            </Border>

        </Grid>
    </Grid>
</Window>
